function Transport_Search_init(transport)
{
    transport.search = function(text)
    {
        
    }

    transport.search_hash = function(hash)
    {
        
    }

    transport.search_peer = function(uid)
    {
        
    }


    transport.addEventListener('search', function(event)
    {
        
    })

    transport.addEventListener('search.hash', function(event)
    {
        
    })

    transport.addEventListener('search.peer', function(event)
    {
        
    })
}